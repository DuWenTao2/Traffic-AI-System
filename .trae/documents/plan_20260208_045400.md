# Implementation Plan: Automatic Stop Mechanism for Headless Video Processing

## 1. Modify VideoProcessorMP Initialization

Update the VideoProcessorMP initialization to set appropriate auto_restart behavior based on interface mode:

```python
def __init__(self, video_id, source, use_stream=False, camera_location="Unknown", coordinates=None, max_speed=60, min_speed=5):
    # Existing initialization code...
    
    # Control for auto-restart of video when it finishes
    # Set auto_restart to False in headless mode for local videos
    self.auto_restart = True
    
    # Existing code...
```

## 2. Update _load_detection_settings Method

Modify the settings loading to adjust auto_restart based on interface mode:

```python
def _load_detection_settings(self):
    # Existing code...
    
    # Default value for video interface control
    self.show_video_interface = True
    
    try:
        if os.path.exists(config_file):
            with open(config_file, 'r', encoding='utf-8') as f:
                config = json.load(f)
                # Existing detection settings loading...
                
                # Load video interface setting
                if "show_video_interface" in config:
                    self.show_video_interface = config["show_video_interface"]
                    print(f"[{self.video_id}] Video interface setting: {'Enabled' if self.show_video_interface else 'Disabled (Linux mode)'}")
                    
                    # In headless mode, disable auto-restart for local videos
                    if not self.show_video_interface and not self.use_stream:
                        self.auto_restart = False
                        print(f"[{self.video_id}] Auto-restart disabled for headless local video processing")
                return merged_settings
        # Existing error handling...
```

## 3. Enhance Video Processing Loop with Stream Interruption Detection

Update the main processing loop to detect stream interruptions and handle automatic stopping:

```python
def run(self):
    # Existing initialization code...
    
    # Add stream interruption detection variables
    stream_interruption_count = 0
    max_stream_interruptions = 5  # Allow 5 consecutive failed frames for streams
    
    while not self.exit.is_set():
        try:
            # Read frame using our VideoReader with error handling
            ret, frame = self.video_reader.read()
            
            # Check if we reached the end of the video or stream interruption
            if not ret or frame is None:
                print(f"[{self.video_id}] End of video reached or frame reading failed")
                
                # Handle stream interruption detection
                if self.use_stream:
                    stream_interruption_count += 1
                    print(f"[{self.video_id}] Stream interruption detected ({stream_interruption_count}/{max_stream_interruptions})")
                    
                    # If in headless mode and multiple stream interruptions, stop
                    if not self.show_video_interface and stream_interruption_count >= max_stream_interruptions:
                        print(f"[{self.video_id}] Maximum stream interruptions reached, stopping processing")
                        break
                    
                    # Small delay before retrying stream
                    time.sleep(0.5)
                    continue
                
                # If auto-restart is enabled and it's not a stream, reset the video
                if self.auto_restart and not self.use_stream:
                    # Existing restart code...
                else:
                    # No auto-restart or it's a stream that can't be restarted
                    print(f"[{self.video_id}] Video playback finished.")
                    break
                    
        except Exception as e:
            # Existing error handling...
            
        # Reset stream interruption count on successful frame read
        stream_interruption_count = 0
        
        # Existing processing code...
```

## 4. Ensure Smooth Stopping and Resource Release

Verify that the cleanup method properly handles resource release:

```python
def cleanup(self):
    """Clean up resources used by the video processor"""
    print(f"[{self.video_id}] Cleaning up resources...")
    
    # Existing cleanup code...
    
    print(f"[{self.video_id}] Cleanup completed.")
```

## 5. Testing Scenarios

Test the implementation with the following scenarios:

1. **Local video in headless mode**: Verify it stops after one complete playback
2. **HTTP stream in headless mode**: Verify it stops after stream interruption
3. **Local video in interactive mode**: Verify it still allows user to press 'q' to stop
4. **HTTP stream in interactive mode**: Verify it continues until user stops
5. **Both modes**: Verify video processing and result saving work correctly

## 6. Benefits

- **Reliable headless operation**: Automatic stopping prevents infinite loops
- **Resource efficiency**: Stops processing when video ends or stream fails
- **Backward compatibility**: Interactive mode behavior unchanged
- **Stream resilience**: Allows brief stream interruptions before stopping
- **Clear logging**: Provides detailed information about stopping reasons

This implementation ensures that headless video processing stops appropriately while maintaining the existing interactive mode behavior.